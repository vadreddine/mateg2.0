// utils/helpers.js

// Vous pouvez ajouter ici des fonctions utilitaires communes à plusieurs parties de l'application.

module.exports = {
    // Exemple de fonction utilitaire
    formatDate: (date) => {
      // Formater la date selon vos besoins
      return new Date(date).toLocaleDateString();
    },
  };
  