// config/auth.config.js

module.exports = {
    secret: 'your_jwt_secret', // Remplacez par votre clé secrète JWT
    jwtExpiration: 86400,      // Durée de validité du token en secondes (ici, 24 heures)
  };
  